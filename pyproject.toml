[project]
name = "bt-speaker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ishelly",
    "pygame>=2.6.1",
]

[tool.uv.sources]
ishelly = { path = "../ishelly", editable = true }
